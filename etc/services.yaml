apiVersion: v1
kind: Service
metadata:
  name: ingress
  namespace: ${NAMESPACE}
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 80
    - name: https
      port: 443
      targetPort: 443
  selector:
    app: nginx
---
apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: ${NAMESPACE}
spec:
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: admin
      port: 9001
      targetPort: 9001
  selector:
    app: minio